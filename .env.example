# Database Configuration
# In production, set these variables in your hosting provider's dashboard.
# In local development, you can create a .env file with these values (optional if using defaults).

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_PORT=3306
DB_NAME=ans_test
